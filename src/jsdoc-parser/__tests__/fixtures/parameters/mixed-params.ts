/**
 * @autodoc POST /articles/{id}/comments
 * @pathParam {string} id Article ID
 * @queryParam {boolean} [notify] Send notification
 * @headerParam {string} Authorization Bearer token
 * @cookieParam {string} [session] Session cookie
 */
